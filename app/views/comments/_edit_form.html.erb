<%= turbo_frame_tag dom_id(@comment) do %>
  <li>
    <p>ユーザー名：<%= comment.user.username %></p>
      <% if current_user.id == comment.user_id %>
        <%= form_with model: [comment.study_record, comment] do |f| %>
          <%= render partial: 'shared/error_messages', locals: { object: comment } %>
          <%= f.text_area :comment %>
          <%= link_to 'キャンセル', study_record_path(comment.study_record) %>
          <%= f.submit '更新' %>
        <% end %>
      <% end %>
    <p><%= l comment.created_at %></p>
  </li>
<% end %>
